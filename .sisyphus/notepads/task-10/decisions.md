## 2026-05-08
- Implemented strict v1 as metadata-only discovery with an explicit `temporal.values` requirement, matching the practical v1 task specification.
- Metadata-only axes use standard EDR CoverageJSON order: `t`, optional `z`, `y`, `x`.
