2026-05-08: `query.encode_bbox` rejects degenerate bboxes (`lon_min >= lon_max` raises `EdrUnsupportedFeatureError`). When an `int` index collapses a spatial axis, format the bbox string manually (`f"{lon_min},{lat_min},{lon_max},{lat_max}"`) instead of going through `encode_bbox`.
2026-05-08: `np.datetime64(t, 's')` triggers mypy `call-overload` errors when `t` is the generic `np.datetime64` because numpy's stubs require literal precision parameters. Workaround: type the helper parameter as `Any` with `# noqa: ANN401`, since values come from `npt.NDArray[Any]` indexing.
2026-05-08: `np.datetime_as_string(val, unit='s', timezone='UTC')` returns numpy `str_`; wrap in `str(...)` for a plain Python string. Format: `2025-01-01T00:00:00Z`.
2026-05-08: `--cov=src/edr_xarray/indexer` (file path) yields "Module was never imported" warning; use the dotted module name `--cov=edr_xarray.indexer` for accurate coverage measurement.
2026-05-08: `@dataclass(frozen=True, eq=False)` is the right combo for dataclasses holding numpy arrays — avoids the array-equality ambiguity error frozen dataclasses would otherwise hit when generating `__eq__`.
2026-05-08: `_is_full_extent` checks raw slice attrs (`start`, `stop`, `step`) without resolving negatives. `slice(-N, None)` on a length-N axis is therefore *not* recognized as full extent — acceptable trade-off; xarray basic indexing rarely emits negative starts.
